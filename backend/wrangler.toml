name = "fragged-api"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Premier-seasons cache. The Worker creates the table on first use (see ensureSchema
# in src/index.js); schema.sql documents the same DDL. Falls back to a live
# csstats.gg scrape if this binding is ever removed.
[[d1_databases]]
binding = "DB"
database_name = "fragged-cache"
database_id = "5a54886d-b33c-47ac-82c4-a6d5921791a0"
